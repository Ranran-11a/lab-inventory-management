name: Apply auth stuck fix patch

on:
  push:
    paths:
      - "patches/auth-stuck-fix.patch"
      - ".github/workflows/apply-auth-stuck-fix.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply patch
        run: |
          git apply patches/auth-stuck-fix.patch
          rm -f patches/auth-stuck-fix.patch .github/workflows/apply-auth-stuck-fix.yml
          rmdir patches 2>/dev/null || true
      - name: Commit updated project
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Handle Supabase auth errors gracefully" || exit 0
          git push
